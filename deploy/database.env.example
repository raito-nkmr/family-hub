# Copy only these PostgreSQL initialization settings to /etc/family-hub/database.env.
# Use a unique production password and keep the destination file out of the repository.
POSTGRES_DB=family_hub
POSTGRES_USER=family_hub
POSTGRES_PASSWORD=replace-with-a-production-password
